import React from 'react';
import { Alert } from 'antd';

const InfoBar = () => {
  return (
    <div style={{ marginBottom: 16 }}>
      <Alert type="info" message="无" />
    </div>
  );
};

export default InfoBar;
